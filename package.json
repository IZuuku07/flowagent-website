{
  "name": "flowagent-business-app",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "description": "FlowAgent full-service website with admin dashboard, payments, and persisted orders.",
  "scripts": {
    "start": "node server.js",
    "start:studio": "node --env-file=.env.local server.js",
    "test": "node --test tests/*.test.cjs"
  }
}